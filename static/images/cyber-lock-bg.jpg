The image data would go here, but since we can't directly create binary files, you'll need to:

1. Save the cybersecurity lock image as "cyber-lock-bg.jpg"
2. Place it in your project's "static/images/" directory 